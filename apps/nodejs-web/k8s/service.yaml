apiVersion: v1
kind: Service
metadata:
  name: nodejs-web
  labels:
    app: nodejs-web
spec:
  selector:
    app: nodejs-web
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
