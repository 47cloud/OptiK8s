apiVersion: v1
kind: Service
metadata:
  name: java-spring
  labels:
    app: java-spring
spec:
  selector:
    app: java-spring
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
